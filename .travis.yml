dist: bionic
os: linux
language: generic

jobs:
  include:
    services:
    - mongodb
    script:
    - cat /etc/mongod.conf
    - sudo cat /var/log/mongodb/mongod.log
    - sudo cat /var/log/syslog
